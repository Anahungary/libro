---
// src/components/Disqus.astro
---

<div id="disqus_thread"></div>

<style is:global>
  /* Ocultar elementos informales de Disqus */
  
  /* Ocultar la sección "What do you think?" */
  .textarea-wrapper::before {
    display: none !important;
  }
  
  /* Ocultar botones de reacciones emocionales */
  .publisher-anchor-color[data-role="reaction-buttons"] {
    display: none !important;
  }
  
  /* Ocultar recomendaciones de Disqus */
  #disqus_recommendations, 
  .recommendations-wrapper {
    display: none !important;
  }
  
  /* Ocultar publicidad */
  #disqus_thread iframe[src*="ads-iframe"] {
    display: none !important;
  }
  
  /* Ocultar botones de compartir en redes sociales */
  .share-buttons {
    display: none !important;
  }
  
  /* Simplificar el footer de Disqus */
  .disqus-footer__wrapper {
    display: none !important;
  }
  
  /* Simplificar los avatares */
  .avatar, .user-avatar {
    border-radius: 3px !important; /* Menos redondos, más profesionales */
  }
  
  /* Reducir espacio y hacer la interfaz más compacta */
  #disqus_thread {
    padding-top: 10px !important;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif !important;
  }
  
  /* Eliminar vínculos de marca/powered by */
  .disqus-powered-by {
    display: none !important;
  }

  #disqus_thread {
  color: #333333 !important;
  background-color: #ffffff !important;
}

#disqus_thread * {
  color: inherit;
  background-color: inherit;
}
</style>

<script is:inline>
    var disqus_config = function () {
        this.page.url = window.location.href;  
        this.page.identifier = window.location.pathname;
        // Usar un tema más profesional
        this.page.colorScheme = 'light';
    };
    
    (function() {
        var d = document, s = d.createElement('script');
        s.src = 'https://https-anaprieto-vercel-app.disqus.com/embed.js';
        s.setAttribute('data-timestamp', +new Date());
        (d.head || d.body).appendChild(s);
    })();
</script>
<noscript>Please enable JavaScript to view the <a href="https://disqus.com/?ref_noscript">comments powered by Disqus.</a></noscript>